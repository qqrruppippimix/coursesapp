<%= form_for(@course_reserf) do |f| %>
  <% if @course_reserf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_reserf.errors.count, "error") %> prohibited this course_reserf from being saved:</h2>

      <ul>
      <% @course_reserf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
