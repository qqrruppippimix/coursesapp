<%= form_for(@course_payment) do |f| %>
  <% if @course_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_payment.errors.count, "error") %> prohibited this course_payment from being saved:</h2>

      <ul>
      <% @course_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
